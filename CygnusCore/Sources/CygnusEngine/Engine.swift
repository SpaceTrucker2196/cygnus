// Facade the app-side CygnusKit and the CLI consume. Register a
// repository, run analysis producing an immutable graph revision,
// query the result. Real API lands as E1–E5 fill in beneath it.

public enum CygnusEngineInfo {
    public static let version = "0.1.0"
}
